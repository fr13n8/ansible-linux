---
- name: Assert supported distribution
  ansible.builtin.assert:
    that:
      - "ansible_pkg_mgr in ('apt', 'dnf', 'pacman')"

- name: 'Install ({{ ansible_pkg_mgr }})'
  include_tasks: 'install-{{ ansible_pkg_mgr }}.yml'